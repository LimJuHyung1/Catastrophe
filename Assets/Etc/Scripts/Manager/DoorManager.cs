using UnityEngine;

public class DoorManager : MonoBehaviour
{
    public AudioClip doorOpenSound; // 문 열기 소리    
    public DoorBase[] hingedDoors;
    public DoorBase[] slidingDoors;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    void SetDoorsSounds()
    {
        
    }
}
